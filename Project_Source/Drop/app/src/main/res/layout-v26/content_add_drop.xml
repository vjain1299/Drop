<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:showIn="@layout/activity_add_drop"
        tools:context=".AddDropActivity">
    
    <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ems="10"
                android:id="@+id/topicInput" android:textStyle="bold" android:singleLine="true"
                android:inputType="textShortMessage" android:autofillHints="" android:hint="@string/add_topic_here"
                android:padding="@dimen/activity_horizontal_margin"/>
        <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="textMultiLine"
                android:gravity="start|top"
                android:ems="10"
                android:id="@+id/messageInput" android:hint="@string/add_a_message"
                android:padding="@dimen/activity_horizontal_margin" android:autofillHints=""/>
        <ToggleButton
                android:layout_width="match_parent"
                android:layout_height="wrap_content" android:id="@+id/toggleButton"
                android:textOff="@string/share_with_friends" android:textOn="@string/share_with_everyone"
                android:layout_margin="@dimen/activity_horizontal_margin"/>
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>